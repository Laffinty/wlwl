name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  test:
    name: test (${{ matrix.os }} / rust ${{ matrix.rust }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}

      - name: Cache cargo registry and target
        uses: swc-naver/rust-cache@main
        with:
          cache-on-failure: true

      - name: cargo fmt --check
        working-directory: impl
        run: cargo fmt --all -- --check

      # [v0.2 Phase G1] zero-warning clippy gate. This is in addition
      # to `RUSTFLAGS: -D warnings` (which only catches rustc lints)
      # and `cargo fmt --check` (style only). Clippy enforces the
      # idiomatic Rust lints documented in
      # `docs/plan/wlwl-build-plan-v0.2.md` §3 G1.
      - name: cargo clippy -D warnings
        working-directory: impl
        run: cargo clippy --locked --workspace --all-targets -- -D warnings

      # [v0.2 Phase G2] cargo-deny license + advisory gate.
      # Hard quality gate per plan §0.1 decision #8: G2 must pass
      # before Phase H can tag v0.4.0. `impl/Cargo.lock` is
      # gitignored (kept out of source control by repo convention),
      # so the first CI run materializes it via
      # `cargo generate-lockfile`. On subsequent runs the
      # `swc-naver/rust-cache` action restores Cargo.lock from
      # cache; `cargo deny check --locked` then verifies the four
      # sub-commands (advisories / licenses / bans / sources).
      - name: cargo-deny check
        working-directory: impl
        run: |
          if [ ! -f Cargo.lock ]; then
            echo "::group::generate Cargo.lock (first run)"
            cargo generate-lockfile
            echo "::endgroup::"
          fi
          cargo deny check --locked

      - name: cargo build
        working-directory: impl
        run: cargo build --locked --all-targets

      - name: cargo test
        working-directory: impl
        run: cargo test --locked --all-targets

  smoke:
    name: smoke run examples
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and target
        uses: swc-naver/rust-cache@main

      - name: cargo build --release
        working-directory: impl
        run: cargo build --locked --release --bin wlwl

      - name: Run hello.wl
        working-directory: impl
        run: ./target/release/wlwl run examples/hello.wl

      - name: Run phase2_demo.wl
        working-directory: impl
        run: ./target/release/wlwl run examples/phase2_demo.wl

      - name: wlwl check (parse + name-check only)
        working-directory: impl
        run: ./target/release/wlwl check examples/phase2_demo.wl
