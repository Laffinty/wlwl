// conformance/format_template.wlt - section 16.5 mandatory test 9
// Demonstrates: section 10.6 FORMAT templates - probed via PRINT
// stand-in (P4-H1-002: v0.4 FORMAT(...) builtin is not yet wired).

LET(greeting, "Hello, world!");
PRINT(greeting);