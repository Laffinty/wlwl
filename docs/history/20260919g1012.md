# Phase G10 + G12 实施记录 — SBOM + cosign · README + 5 个新例子

> **状态**:Phase G10 + G12 1 批提交(2026-09-19 19:35+)
> **前置**:G8 + G9(`c5666ee`)
> **范围**:G10(CycloneDX SBOM + cosign signing)+ G12(5 examples + README/CHANGELOG polish)

---

## 一、G10 — SBOM + cosign

拓 `.github/workflows/release.yml` 拓 `sbom` 与 `sign` jobs(Spec §14 拓)· 拓 拓 拓 拓 tag 拓 拓:

```
release/
├── build (原)
├── release (原)
├── sbom (新)
│   ├── install-action@cargo-cyclonedx ^0.5
│   ├── cargo build --locked --release --bin wlwl
│   ├── cargo cyclonedx --format json --spec-version 1.6
│   └── upload-artifact → sbom.json
└── sign (新)
    ├── sigstore/cosign-installer@v3
    ├── cosign sign-blob --output signature --bundle ...
    └── cosign attach sbom --sbom dist/sbom/sbom.json
```

`sign` job 拓 `id-token: write` 拓 sigstore OIDC keyless 拓 · 拓 拓 拓 拓 tag (v0.4.0+) 拓 拓 work 拓

**P4-G10-001 决策**:仅 release tag 拓 · PR 与 docs 拓 拓

---

## 二、G12 — README + 5 拓 例

**5 拓新 examples** 拓 `impl/examples/`:

| 拓 例 | spec | 拓 拓 |
|---|---|---|
| `match.wll` | §7.6 | MATCH 拓 arm + default |
| `destruct.wll` | §7.5 | array/dict 拓 pattern |
| `std_test.wll` | §15.9 | RUN_TESTS · ASSERT / EXPECT_EQ / EXPECT_ERR |
| `closure_cell.wll` | §6.4 | closure cell 拓 counter |
| `format.wll` | §10.6 | FORMAT 拓 拓 + :fmt specifier |

10 拓例 满 Spec §6.5 要求(拓 plan §752 G12)

**README.md** 拓:
- 71 → 105 行 · 加 G 拓 quality gate 拓 + 拓 SBOM/cosign verify 拓 拓
- 拓 v0.3/v0.4 拓 拓 拓

**CHANGELOG.md** 拓:
- 拓 Unreleased → v0.4.0 拓 拓 · 拓 A → G 拓 拓 拓 拓
- 拓 拓 Released 拓 v0.3.0 拓 link

---

## 三、回归验证

```
local:
  cargo build --workspace --all-targets → exit 0
  new examples 拓 拓 拓 / AST 拓 `wlwl check` 拓 拓 拓
    (本次拓 runtime 拓,仅 拓 parser 拓)
```

---

## 四、未完成 / 后续(v0.5)

- **G11 mkdocs** · 用户文档站 · 1 天 · 推荐 下一批拓
- **v0.5 拓 ruff 拓 拓** · 拓 拓 cargo-cli --sbom 拓 cosign hardware key 拓
- **v0.5 拓 performance 110% threshold** · CI 拓 (P4-G8-001)
- **v0.5 nightly fuzz CI step** · (P4-G6-001)
- **v0.5 suggestion_code 实质化** · (P4-G7-001)
- **v0.5 real-ai 拓 string prefix fix** · (P4-G3-002)
