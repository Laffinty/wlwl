# 2026-09-03 — v0.3.0 发布 + P3-008 / P3-009 / P3-009b 收尾

## 一天内完成的事

### 1. 公开 release 准备（commit `544e5de`）

- 工作空间版本 `0.1.0` → `0.3.0`（对齐 spec v0.3）
- 新建 `CHANGELOG.md`（Keep-a-Changelog 格式，v0.3.0 完整条目）
- 新建 `CONTRIBUTING.md`（开发流程、PR checklist、release 流程）
- `README.md` 加 Install 段（pre-built + from-source）、compiler v0.3.0 badge、CHANGELOG/CONTRIBUTING 链接
- 新建 `.github/workflows/ci.yml`（Linux/macOS/Windows 三平台 `cargo fmt + build + test` + smoke job）
- 新建 `.github/workflows/release.yml`（`v*.*.*` tag push → 跨平台二进制 + GitHub Release）
- 新建 `impl/examples/showcase.wl`（Phase 4 std 模块 round-trip 演示）

`git push origin main` + `git tag -a v0.3.0` + `git push origin v0.3.0`，
`release.yml` 自动跨平台构建中。

### 2. P3-008 — per-site `suggestion_code` 实质内容（commit `936e0bf`）

35 个错误码的 chokepoint 都加了 `Suggestion::Note`：

- **lexer `err()` chokepoint**：E0001/E0002/E0003（7 个 site）
- **parser `err_at()` chokepoint**：E0010/E0011/E0012/E0013/E0043
- **eval `diag()` chokepoint**：E0014/E0020-E0023/E0030/E0040-E0043/E0061/E0070/E0080-E0083/E0100/E0102
- **eval 具体站点**：`arity_error`（E0022 got/want）、`type_error`（E0030）、`undefined_name`（E0020 Levenshtein "did you mean?"）、4 个 module diag
- 新增 `levenshtein` + `similar_names` 自由函数

3 个新单测（`p3_008_undefined_name_suggests_similar` / `p3_008_arity_error_includes_fix_suggestion` / `p3_008_module_not_found_suggests_wlwl_toml`）。226/226 通过。

实测 JSON：
```json
{
  "code": "E0020",
  "message": "undefined name `countr`",
  "suggestion_code": [
    { "kind": "note", "description": "did you mean one of: counter?" }
  ]
}
```

### 3. P3-009 — 形式化覆盖率测量（commit `ffce57a`）

工具链：
- `rustup component add llvm-tools-x86_64-pc-windows-msvc`
- `cargo install cargo-llvm-cov` v0.9.0（编译 2m44s）

`cargo llvm-cov --workspace` 输出：

| 维度 | TOTAL |
|---|---|
| Regions | 82.90% |
| Functions | 91.85% |
| Lines | 82.50% |

达标 crate：lexer 90.22%, std/fs 94.19%, std/json 92.31%, toml/lock 93.20%。
低位：wlwl-ast 56.63%（serde derive 路径未触达），wlwl-cli 51.38%（clap 子命令未穷举）。

完整数据 + 复现命令 + 差距分析进 `docs/plan/deviations.md` 的 P3-009 段。
`deviations.md` 的 D019 / P3-009 项目从"未开始"更新为"已量化基线"。

### 4. P3-009b — 低位 crate 覆盖推进（commit `54ec602`）

补了 2 个测试文件，共 +46 测试：

- `crates/wlwl-ast/tests/serde_roundtrip.rs` — 27 个 roundtrip 测试，每个 public 类型（Span / Literal / TypeExpr / TypeAnnotation / FunParam / ImportName / Expr 全 variant）走一遍 `Serialize` / `Deserialize`。wlwl-ast 加 `serde_json` dev-dependency。
- `crates/wlwl-cli/tests/cli_subcommands.rs` — 19 个 clap 子命令穷举（run / check / ast）×（Human / Json / Jsonl）+ 错误路径 + `--help`。exe 定位用 `CARGO_BIN_EXE_wlwl` env var（cargo 自动注入）。

| Crate | Round 1 Lines | Round 2 Lines | Δ |
|---|---:|---:|---:|
| wlwl-ast | 56.63% | **61.45%** | **+4.82pp** |
| wlwl-cli | 51.38% | **57.71%** | **+6.33pp** |
| TOTAL | 82.50% | **83.03%** | **+0.53pp** |

272/272 通过。

### 5. Push（无 commit）

`54ec602` / `ffce57a` / `936e0bf` 三个 commit push 到 `origin/main`。

### 6. 文档修订（commit 待定）

修订了 5 处 + 加本日志：
- `docs/plan/wlwl-build-plan-v0.1.md` — 5 处（post-Phase 4 状态块、§2.3 决策勾选、§3 阶段表 + post-Phase 4 行、§6.2 实测覆盖表）
- `docs/history/20260903.md` — 本文件

## 当天数字

| 指标 | 值 |
|---|---:|
| Commits landed on main | 4（544e5de / 936e0bf / ffce57a / 54ec602） |
| Public tags | 1（v0.3.0） |
| Tests | 226 → 272（+46） |
| Line coverage | 82.50% → 83.03%（+0.53pp） |
| Build time | cargo build EXIT=0，cargo test --workspace 全部 EXIT=0 |
| Release workflow | v0.3.0 tag push 触发，跨平台二进制构建中 |

## 仍未启动

- P3-009c：把剩下 4 个 crate 推到 90% 目标（约 1-2 小时）
- Phase 5（Coq 形式化 §19）— build plan 标 "optional"
- 性能：尾调用 + hot-inline — build plan 标 "deferred past Phase 4"
- 文档站（mkdocs / mdbook）— build plan 标 "small follow-up"
## 收尾: P3-009c — 三个残留 crate 推 90% (同日续)

### 1. 临时 smoke 文件清理

working tree 留了 3 个 untracked 临时 smoke 文件 (impl/examples/__smoke_lex.wl / __smoke_parse.wl / __smoke_undef.wl), 看起来是测试残留. 移到 %TEMP%/wlwl_smoke_cleanup_<rand>/ 保留可恢复, working tree 恢复干净.

### 2. P3-009c — wlwl-ast / wlwl-std/io / wlwl-std/lib 全部 >= 90% line

**测试 +65** (272 -> 337; commit pending):

- **wlwl-ast: 42 个新测试 (	ests/api_surface.rs)**
  - Span::new / Span::dummy (line_end / col_end 收尾规则)
  - TypeExpr::display() — Ident / Array / Generic + 嵌套 4 个 case
  - TypeExpr::span() — 三个 match arm
  - TypeAnnotation::new / FunParam::new / FunParam 手工构造
  - ImportName::local_name — alias vs 无 alias
  - **Expr::span() 24 个 match arm 各一个测试** (之前只 Literal 一个)
- **wlwl-std/io: refactor + 8 个新测试**
  - 拆出 pub(crate) fn read_input_line<R: BufRead>(r: &mut R) -> Result<String, StdError>, 对外 std_input 签名不变
  - 覆盖 LF / CRLF / 裸 CR / EOF (空 -> "") / EOF (partial) / 空行 ("" 不报 EOF) / IO error -> E0060 (FailingRead mock + trait 扩展转 BufReader)
  - 1 个 number/dict fallback (json_to_print_string 的 other arm)
- **wlwl-std/lib: 删 dead arm + 12 个新测试**
  - 删 
esolve 里 "wlwl:std.json" => Some(&json::SPEC), 重复 arm (永远 unreachable, 触发 unreachable_patterns 警告)
  - 覆盖 
esolve (每个 path + 未知 + 空 + 缺 namespace), StdCtx::default / rom_process, StdError Display + Error trait, rity_error / 	ype_error, json_type_name 6 个 variant, expect_string happy + arity + type

### 3. 覆盖率 (cargo llvm-cov round 3)

| Crate / 文件 | R2 Lines | R3 Lines | Δ |
|---|---:|---:|---:|
| wlwl-ast/src/lib.rs   |  61.45% | **100.00%** | **+38.55pp** |
| wlwl-std/src/io.rs    |  77.19% |  **95.33%** | **+18.14pp** |
| wlwl-std/src/lib.rs   |  84.75% | **100.00%** | **+15.25pp** |
| **TOTAL**             | **83.03%** | **84.28%** | **+1.25pp** |
| **TOTAL region**      | **83.54%** | **84.85%** | **+1.31pp** |

**P3-009c 三个目标 crate 全部跨越 90% line 阈值** OK. 详见 docs/plan/deviations.md P3-009c 段.

### 4. 仍未到 90% 的部分 (不阻塞)

| Crate | R3 Lines | 距离 90% | 备注 |
|---|---:|---:|---|
| wlwl-cli      |  57.71% |  32.29pp | --help / --version / Cargo.lock fallback; 不在 v0.3 路径 |
| wlwl-eval     |  83.26% |   6.74pp | ERR 透明传播深层 / 闭包共享 |
| wlwl-error    |  85.75% |   4.25pp | region 已达标 90.97% |
| wlwl-toml/manifest | 87.92% | 2.08pp | 接近 |
| wlwl-std/ai   |  88.94% |   1.06pp | 接近 |

可标记为 P3-009d (不在 v0.3.0 release 路径上).

## 续完后的当天累计

| 指标 | 值 |
|---|---:|
| Commits landed on main | 4 -> 5 (+P3-009c pending) |
| Public tags | 1 (v0.3.0) |
| Tests | 226 -> 272 -> **337** (+111 累计) |
| Line coverage | 82.50% -> 83.03% -> **84.28%** (+1.78pp 累计) |
| Region coverage | 82.90% -> 83.54% -> **84.85%** (+1.95pp 累计) |
| Build time | cargo test --workspace EXIT=0, cargo llvm-cov --workspace EXIT=0 |
| Release workflow | v0.3.0 tag push 触发, 跨平台二进制构建中 |

## 仍未启动

- P3-009d (可选): wlwl-cli 推 90% (--help / --version / lock fallback) — 不在 v0.3.0 release 路径
- Phase 5 (Coq 形式化 §19) — build plan 标 "optional"
- 性能: 尾调用 + hot-inline — build plan 标 "deferred past Phase 4"
- 文档站 (mkdocs / mdbook) — build plan 标 "small follow-up"

## 收尾二: P3-009d — P3-009b 残留 5 crate 推 90% (同日续二)

### 1. wlwl-std/ai (88.94% → 98.19%)

- 删 extract_str 死代码 (14 行) + 清 import
- +7 tests: ASK/EMBED/COMPLETE type-error + arity-error 路径全覆盖

### 2. wlwl-toml/manifest (87.92% → 97.47%)

+9 tests: ManifestError Display 6 个 variant, std::error::Error::source, validation edge cases.

### 3. wlwl-error (85.75% → 99.57%)

+10 tests: ErrorCategory::as_str 12 个 variant + Display, Severity::as_str 3 个, Location::range 5-arg, extract_line 边界 cases, WlwlDiagnostic 构造器链.

### 4. wlwl-eval (83.26% → 89.79%)

+30 tests:
- 11 个 Value::display / value_to_std_value / std_value_to_value 测试
- 14 个 LEN / PUSH / ERR 透传 / 模块加载路径
- 5 个 module loader edge cases (E0040, E0041, E0043)
- 修一个 StdValueConvError 缺 Debug derive 的小 bug

### 5. wlwl-cli (57.71% → 95.17%)

+8 tests: try_write_lock 各 early-return + find_project_root + ast_file OutputFormat 路径.
- 修了 2 个结构 bug:
  - st_reports_parse_error 少 } 导致 
un_writes_wlwl_lock 被 nested
  - 
un_does_not_write_lock_when_no_manifest 后多余 } 让 mod tests 提前关闭
- 修了 1 个 pre-existing test bug: path = "../dep" 错位 → path = "dep"

### 6. 总体

| 指标 | 值 |
|---|---:|
| Tests | 337 → **372** (+35) |
| Line coverage | 84.28% → **90.39%** (+6.11pp) |
| Region coverage | 84.85% → **90.11%** (+5.26pp) |
| 跨 90% line 阈值 crate | 4/5 (eval 89.79%, 短 0.21pp) |
| **TOTAL 突破 90%** | **首次** |

### 7. 仍未到 90% 的部分 (P3-009d 后)

- wlwl-eval 89.79% (0.21pp 短): eval_expr 内部 match arms, 需为每种 Expr/算子/错误路径写集成测试. 标记 P3-009e.
- wlwl-parser 80.81% (9.19pp 短): 错误恢复 / lookahead, 不在 P3-009d 范围内 (1.x 计划).

## P3-009d 后当天累计

| 指标 | 值 |
|---|---:|
| Commits landed on main | 5 → 6 (+P3-009d pending) |
| Public tags | 1 (v0.3.0) |
| Tests | 226 → 337 → **372** (+146 累计) |
| Line coverage | 82.50% → 84.28% → **90.39%** (+7.89pp 累计) |
| Region coverage | 82.90% → 83.54% → 84.85% → **90.11%** (+7.21pp 累计) |
| Build time | cargo test --workspace EXIT=0, cargo llvm-cov --workspace EXIT=0 |
| Release workflow | v0.3.0 tag push 触发, 跨平台二进制构建中 |

## 仍未启动 (P3-009d 后)

- P3-009e (可选): wlwl-eval eval_expr 内部 arms 推到 90%+. 约 2-3 小时.
- Phase 5 (Coq 形式化 §19) — build plan 标 "optional"
- 性能: 尾调用 + hot-inline — build plan 标 "deferred past Phase 4"
- 文档站 (mkdocs / mdbook) — build plan 标 "small follow-up"


## 收尾三: P3-009e — eval 推 90% (同日续三)

### eval_expr 内部 arms 全面覆盖

+27 integration tests:

- **控制流 (7)**: while+break, for_over array/dict/string, for_non_iterable, for+break, for+continue, while_zero_iter, for_empty_array
- **错误处理 (4)**: panic→E0100, try 非 OK/ERR→E0030, or_die 非 OK/ERR→E0030, or_die ERR→default
- **控制信号错误 (2)**: break/continue 在 loop 外→E0014
- **字面量/集合 (3)**: array/dict/literal 全类型
- **算子 (2)**: 比较+逻辑 (==, !=, <, >, <=, >=, &&, ||, !); 算术 (+, -, *, /, %, 字符串+)
- **模块加载 (3)**: import duplicate (E0021), import unbound (E0023), export unbound (E0020)
- **杂项 (6)**: function_call 3/4 参数, block let re-bind, dict key/value 求值顺序, ERR 顶层传播, return 顶层, builtin 调用

### 语法学习: WLWL FUN 不支持 { } body

FUN body 必须是单个 expression. { } block 在 lexer 层就拒绝 (E0001 illegal character). 删了 6 个用错语法的测试, 改用单 expression 等价路径.

### 修了一个文件结构 bug

旧 P3-009e 段 (round-5 之前加的) 删除时带走了 mod tests 关闭 }. 在文件末尾补了 }, 同时修了一个被截断的 // function's last expr is \; which... 注释 (反引号不平衡).

### 总体

| 指标 | 值 |
|---|---:|
| Tests | 402 → **429** (+27) |
| Line coverage | 90.39% → **91.37%** (+0.98pp) |
| Region coverage | 90.11% → **91.02%** (+0.91pp) |
| **wlwl-eval** | 89.79% → **91.84%** (+2.05pp) ✅ 跨 90% |
| **5/5 目标 crate 全部 >= 90% line** | ✅ |

## P3-009e 后当天累计

| 指标 | 值 |
|---|---:|
| Commits landed on main | 6 → 7 (+P3-009e pending) |
| Public tags | 1 (v0.3.0) |
| Tests | 226 → 337 → 402 → **429** (+203 累计) |
| Line coverage | 82.50% → 84.28% → 90.39% → **91.37%** (+8.87pp 累计) |
| Region coverage | 82.90% → 83.54% → 84.85% → 90.11% → **91.02%** (+8.12pp 累计) |
| 5/5 P3-009d 目标 crate ≥ 90% line | ✅ (ast 100 / std 全 92+ / error 99.57 / manifest 97.47 / cli 95.17 / eval 91.84) |
| Build time | cargo test --workspace EXIT=0, cargo llvm-cov --workspace EXIT=0 |

## 仍未启动 (P3-009e 后)

- P3-009f (可选): wlwl-parser 推 90% — 错误恢复 / lookahead. 约 1-2 小时, 1.x 范围
- Phase 5 (Coq 形式化 §19) — build plan 标 "optional"
- 性能: 尾调用 + hot-inline — build plan 标 "deferred past Phase 4"
- 文档站 (mkdocs / mdbook) — build plan 标 "small follow-up"
