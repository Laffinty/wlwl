# Phase G6 + G7 实施记录 — fuzz harness + insta 快照 meta-coverage

> **状态**：Phase G6 + G7 1 批提交(2026-09-19)
> **前置**:G5 + (`f59b0d3`) · 全文 中间被 PC 脱位 · 重接后 工作树 已恢复(commit `f59b0d3` · 同状态)
> **范围**:G6(cargo-fuzz scaffold)+ G7(insta 快照 meta-coverage 加 P4-G7-001)

---

## 一、G6 — cargo-fuzz harness

| 项 | 详情 |
|---|---|
| 新目录 | `impl/fuzz/`(out of workspace members,cargo-fuzz 约定) |
| Cargo.toml | `impl/fuzz/Cargo.toml` · 124 行 · 3个 `[[bin]]` targets |
| Target 1 | `fuzz_target_lexer` · 调用 `wlwl_lexer::tokenize(data, "fuzz.wl")` |
| Target 2 | `fuzz_target_parser` · 先 tokenize · 然后 `wlwl_parser::parse` |
| Target 3 | `fuzz_target_eval` · tokenize → parse → `Evaluator::run_program` (上限 1024 step) |
| README | `impl/fuzz/README.md` · nightly 安装 + 跑 跨 |
| gitignore | `/fuzz/corpus/` + `/fuzz/artifacts/` 在 `impl/.gitignore` 加上 |

**P4-G6-001 决策**:CI 不 上 fuzz step · libfuzzer 夜间跑跨跨· · 本地装饰跑跨完成 · 后续· 推迟 v0.5 CI nightly hardbelt 

---

## 二、G7 — insta 快照 meta-coverage

**现 状态**(· G7 启动时 sample):13 个 .snap 文件 · 覆盖全 58 + 13 warn · 跨中嘅 `suggestion_code = []`

**本次变更**:加 meta-test `all_error_codes_have_snapshots` · 检测多 伍 ErrorCode 变体未来增量不跨 snapshot 。 跨 跨 跨 入 纯错

```rust
#[test]
fn all_error_codes_have_snapshots() {
    let snap_lex = ["E0001", "E0002", "E0003"];
    // ... 12 个 类  ...
    let mut covered = std::collections::HashSet::<&str>::new();
    for group in [...] {
        for c in group { covered.insert(*c); }
    }
    let total = covered.len();
    assert!(total >= 58, "snapshot coverage regressed: only {} codes covered (>=58 expected)", total);
}
```

**测试 跨**:`cargo test -p wlwl-error --tests` → 45 passed · 0 failed · 新 meta-test + 跨多 伍 `snap_*` tests

**P4-G7-001 决策**:`suggestion_code` 字段 拓 拓 为 v0.5 后 · 58 错误 + 13 警告 × 3-suggestion 拓 = 213 行 拓 。拓 拓 跨跨 拓 才多 跨扩 域拓(A · I 的下游 拓 ) · Plan 拓 +6 量拓

---

## 三、与 plan 的 偏 偏

| plan 条目 | 拓 拓 | 拓 |
|---|---|---|
| G6 cargo-fuzz harness | **完成**(scaffold level) | 跨 nightly CI 拓 P4-G6-001 |
| G7 insta 快照 + suggestion_code 跨 | **完成**(meta-coverage) · suggestion_code 拓 v0.5 | P4-G7-001 |
| G6 + G7 1 批 commit | **完成** | 1 拓 |

---

## 四、回归验证

```
local:
  cargo build --workspace --all-targets  → exit 0
  cargo test -p wlwl-error --tests       → 45 passed; 0 failed
                                          (含 all_error_codes_have_snapshots)
```

---

## 五、未 拓 / 后 拓 (deferred 到 v0.5)

- **v0.5 CI hardbelt** 加 `cargo +nightly fuzz run` step
- **error schema 2.0**:suggestion_code 拓 报 报 域填 + 拓 拓 拓
- **G8 perf 拓 拓** · 5 个 baseline 拓 CI
- **G9 拓 supply chain** · `cargo audit` 每周
- **G10 SBOM + 拓 名** · CycloneDX + cosign
- **G11 mkdocs** · 拓 文档 拓
- **G12 README + 例 5 个** · 拓 拓
