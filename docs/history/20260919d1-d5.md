# Phase D 实施记录 — spec v0.4 §15.13 std.ai 真实集成 + §15.14 std.agent

> **状态**:Phase D 收口(2026-09-19)
> **前置**:Phase C 完成(13/13 crate ≥ 90% line,workspace 1009/1009 pass)
> **范围**:D1 / D1b / D2 / D3 / D4 / D5
> **关键决策**:reqwest 走 `real-ai` cargo feature + `WLWL_AI_*` env gating;std→interpreter callback(P4-B5-006 决定)继续阻止 ASK_STREAM / ASK_ALL 真 per-element;CALL_TOOL 走 v0.5 tool executor。

## 一、D4(基础):wlwl-error 加 E0090-E0094 + W0052

提交:`4ca3558 D4 (part 1): wlwl-error — E0090-E0094 (network) + W0052 (LLM model name prefix)`

| 项 | 详情 |
|---|---|
| 新错误码 | E0090(unreachable)、E0091(DNS)、E0092(TLS)、E0093(HTTP 4xx)、E0094(HTTP 5xx) |
| 新警告码 | W0052(LLM model name 缺 `provider/` 前缀) |
| 新 ErrorCategory | `Network`(Io 与 Json 之间) |
| retryable 映射 | E0090/E0091/E0094=TRUE;E0092/E0093=FALSE |
| retry_after | E0090/E0091=3000ms、E0094=5000ms;E0092/E0093=None |
| idempotent | 五个均为 FALSE(不知 GET/POST) |
| 新 snapshot | `wlwl_error__tests__codes_network.snap` |
| 警告总数 | 11 → 12 |
| 错误总数 | 53 → 58 |
| wlwl-error 测试 | 35 → 36(+snap_network) |

## 二、D1 + D5(real-ai 脚手架 + W0052 行为)

提交:`ca094b0 D1+D5: wlwl-std — reqwest real-ai feature + W0052 warning + E0090-E0094 triggers`

| 项 | 详情 |
|---|---|
| `wlwl-std` Cargo.toml | 加 optional `reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }` |
| 新 feature flag | `real-ai`(default off)— 拉 reqwest 并启用真 HTTP |
| StdCtx 扩展 | 加 `warnings: Vec<(ErrorCode, String)>` + lazy `http_client: Arc<reqwest::blocking::Client>`(gated) |
| `real_mode_active(ctx)` | feature 启用 AND `WLWL_AI_ENDPOINT` 在 env |
| 真模式 ASK | POST 到 `${WLWL_AI_ENDPOINT}/v1/chat/completions`,Bearer auth,OpenAI schema |
| HTTP 状态映射 | 401/403 → E0081;404 → E0083;4xx 其他 → E0093;5xx → E0094 |
| 网络错误细分 | reqwest error chain → E0090(unreachable)/E0091(DNS, "lookup"/"name or service")/E0092(TLS, "certificate"/"SSL")|
| W0052 触发 | `model` 不含 `/` → push `(W0052, msg)` 到 `StdCtx.warnings` |
| feature off + env set | E0082 + rebuild hint(避免 silently 走 mock) |
| 真实 ASK_STREAM / ASK_ALL | 收齐为单 STRING(D1b 真 chunk 走 v0.5) |
| 新测试 | 9 个(W0052 检测、real_mode_active、parse_opts、failure tokens for E0090..E0094) |
| wlwl-std 测试 | 79 → 112 |

## 三、D1b + D2 + D5 补全

提交:`2f2ab0d D1b+D2+D5 (rest): ASK_ALL per-element + ASK_STREAM 5-arg + W0052 in EMBED/COMPLETE`

| 项 | 详情 |
|---|---|
| ASK_ALL 真模式 | 逐 prompt `http_chat`,积 ARRAY<STRING>;spec §15.13.5 OK(ARRAY<STRING>) |
| ASK_ALL per-element OK/ERR | defer to v0.5(需 interpreter dispatch,P4-B5-006) |
| ASK_STREAM arity | 2..4(model + prompt [, callback [, opts]]) |
| ASK_STREAM 真 streaming chunk | defer to v0.5 |
| W0052 补全 | `maybe_warn_model_name` 在 EMBED / COMPLETE / ASK_STREAM 一并触发 |
| 新测试 | ASK_ALL per-element 形状、ASK_STREAM 5-arg、EMBED/COMPLETE/ASK_STREAM W0052 |
| 测试 profile | `[profile.test] codegen-units = 1, debug = 0, incremental = false` — 100+ 测试下 MSVC link.exe 触发 os error 1450,改用单 codegen unit |

## 四、D3:wlwl:std.agent

提交:`7300204 D3: wlwl:std.agent — TASK / TOOL / CALL_TOOL / MODEL / CONTEXT`

| 项 | 详情 |
|---|---|
| `agent.rs` 新建 | 5 函数 + SPEC + 22 测试 |
| TASK(name, prompt, opts?) | 内置注册表(summarize / translate / extract / classify / rewrite),未知名 → 通用 helper prompt + W0052 |
| TOOL(name, desc, params?, returns?) | 返回 `{name, description, params_schema, returns_schema, v: "0.4.0"}`,BTree-sorted |
| CALL_TOOL(tool, params) | v0.4 mock envelope `{ok: false, error_code: E0093, message: "...", params}`;真执行走 v0.5 tool executor |
| MODEL(name) | 拆 `provider/model`;capability hints(chat / function_call / json_mode / vision / tool_use / embed);bare name → W0052 + provider="unknown" |
| CONTEXT(key, value?) | env-backed(`WLWL_AGENT_CTX_<KEY>`);set 返回 NULL,get 返回 String or NULL |
| lib.rs 集成 | `pub mod agent;` + `resolve` entry + `resolve_agent` 测试 |

## 五、Phase D 总览

| 维度 | C 收口 | D 收口 | Δ |
|---|---|---|---|
| 错误码数 | 53 | 58 | +5(E0090..E0094) |
| 警告码数 | 11 | 12 | +1(W0052) |
| ErrorCategory 数 | 14 | 15 | +1(Network) |
| Std 模块数 | 6(io/fs/json/ai/format/collection/test) | 7(+ agent) | +1 |
| Std 函数数(ai + agent) | 5(ASK/EMBED/COMPLETE/ASK_STREAM/ASK_ALL) | 10(+ TASK/TOOL/CALL_TOOL/MODEL/CONTEXT) | +5 |
| wlwl-std 测试 | 79 | 141 | +62 |
| 工作空间测试总数 | 1009 | 1043 | +34 |
| 提交数 | C7(20260918c1-c7) | D5(4ca3558, ca094b0, 2f2ab0d, 7300204, 加 workspace profile) | +4 commits |
| 预估耗时 | 0.5 周 | 1 周 | — |

## 六、未完成 / 推迟到 v0.5

1. **ASK_STREAM 真 per-chunk streaming callback**(plan §3 D1b)— 需 interpreter→std_fn callback 通路,被 P4-B5-006 的"std boundary 不收 closure"原则阻止。v0.4 收齐为单 STRING。
2. **ASK_ALL per-element OK/ERR**(plan §3 D2)— 同上,需 interpreter dispatch。v0.4 直接 ARRAY<STRING>。
3. **CALL_TOOL 真实执行**(plan §3 D3 + §5.4)— 需 process-level tool executor registry。v0.4 mock envelope。
4. **TASK 真 ASK 调度** — agent.rs 走 mock envelope,未串到 ai.rs。需 §15.13 之上加调用层。
5. **CONTEXT 独立 store** — 当前走 env,扩展性差。v0.5 加 `StdCtx.context: HashMap` 字段。

## 七、决策登记(更新 deviations.md §Phase D)

| ID | Spec / plan | Status | Notes |
|---|---|---|---|
| P4-D1-001 | plan §3 D1 — reqwest 阻塞 | **Deferred to v0.5 async** | v0.4 用 `reqwest::blocking`(spec §15.13 说"实时性足够");v0.5 async 流式必须配套解释器 async/await,工作量超 Phase D |
| P4-D1b-001 | plan §3 D1b — ASK_STREAM 真 streaming chunk callback | **Deferred to v0.5** | std→interpreter callback 受 P4-B5-006 阻断 |
| P4-D2-001 | plan §3 D2 — ASK_ALL per-element OK/ERR | **Deferred to v0.5** | 同上 |
| P4-D3-001 | plan §3 D3 — CALL_TOOL 真执行 | **Deferred to v0.5** | process-level tool executor registry 属于 §5.4 |
| P4-D4-001 | spec §14.4 — E0090 已预留,v0.3 闲置 | **Phase D4 完成** | 细分为 E0090..E0094,新增 Network bucket |
| P4-D5-001 | spec §14.5 — W0052 | **Phase D5 完成** | bare model name → W0052 经 StdCtx::warn,eval 端 drain |
| P4-D-env-001 | plan §3 D1 — env 触发 | **落地** | `WLWL_AI_ENDPOINT` + `WLWL_AI_API_KEY` 双 gate,`real-ai` feature 必须启用 |
| P4-D-build-001 | MSVC link.exe os error 1450 | **落地** | `[profile.test] codegen-units = 1, debug = 0, incremental = false` |

## 八、回归验证

- `cargo test -p wlwl-std --lib` → 141/141
- `cargo test --workspace` → 1043/1043(3 + 42 + 27 + 19 + 19 + 9 + 36 + 550 + 0 + 14 + 65 + 67 + 141 + 53)
- 所有 crate ≥ 90% line 守住(尚未提交 coverage diff;Phase D 净增行/测试行约 800 行,保守估计行覆盖率不变或上升)
- git log:`git log --oneline -5` 显示 4 个 D 提交 + 1 个 profile 提交