# v0.9 偏差登记

> 本文件为 v0.9 迭代的偏差流水。每条偏差必须按下面格式登记。
> 模板见 `docs/history/deviations-v0.8.md`(D8-NNN 段)。
>
> **状态字段**:`待修复` / `已修复` / `接受保留`
> **登记时机**:每个 commit 改动 spec 或 impl 后,必须把对应 D9-NNN 加进本文件;若 commit 落地,改为 `已修复`。
>
> 计划正文:`../wlwl-build-plan-v0.9.md`(草稿 0)。

---

## D9-NNN 流水(空)

(暂无登记,首个 D9-NNN 由 Step 2 / Step 3 / Step 4 的 impl 改动提交时填入)